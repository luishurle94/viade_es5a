/**
 * Add image
 * @param {String} milestoneId 
 * @param {Image} image 
 */
export const addImage = async (milestoneId, image) => {
}

/**
 * Add video
 * @param {String} milestoneId 
 * @param {Video} video 
 */
export const addVideo = async (milestoneId, video) => {
}